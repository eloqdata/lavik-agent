#!/bin/sh
set -e
if [ "$1" = purge ]; then
  deb-systemd-helper purge lavik.service >/dev/null || true
fi
if [ -d /run/systemd/system ]; then
  systemctl daemon-reload
fi
# Deliberately retain the service user, database and logs, including on purge.
